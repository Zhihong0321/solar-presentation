{
  "name": "solar-presentation",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix web ci --include=dev && npm --prefix web run build",
    "start": "npm --prefix web start"
  }
}
